# 实验室预约管理系统 - 修改记录

## 修改日期
2026-06-10

---

## 一、文件修改清单

### 1. 前端Logo文件
- **原文件**: `ruoyi-ui/src/assets/logo/meetingLogo.jpg`
- **新文件**: `ruoyi-ui/src/assets/logo/labLogo.jpg`
- **修改原因**: 文件名包含"meeting"，不符合实验室系统命名

### 2. Logo.vue 引用路径
- **文件**: `ruoyi-ui/src/layout/components/Sidebar/Logo.vue`
- **修改内容**: 第17行
  - 原: `import logoImg from '@/assets/logo/meetingLogo.jpg'`
  - 新: `import logoImg from '@/assets/logo/labLogo.jpg'`

### 3. SQL文件修改
- **文件**: `sql/meetinSQL.sql`
- **修改内容**:
  - 第116行: `'会议开始时间'` → `'预约开始时间'`
  - 第117行: `'会议结束时间'` → `'预约结束时间'`
  - 第118行: `'会议目的'` → `'预约目的'`
  - 第119行: `'会议详细描述'` → `'预约详细描述'`
  - 第750行: `'我预定的会议'` → `'我的预约'`
  - 第752行: `'修改预定会议'` → `'修改预约'`
  - 第753行: `'删除我预定的会议'` → `'删除预约'`
  - 第755行: `'与我有关的会议'` → `'预约记录'`
  - 第756行: `'查询关于我的会议详细'` → `'查看预约详情'`

### 4. package-lock.json
- **文件**: `package-lock.json`
- **修改内容**: 第2行
  - 原: `"name": "meeting-room-booking-master"`
  - 新: `"name": "lab-reservation-system"`

---

## 二、数据库修改

### 1. 验证码配置
```sql
UPDATE sys_config SET config_value = 'true' WHERE config_key = 'sys.account.captchaEnabled';
```

### 2. 管理员头像
```sql
UPDATE sys_user SET avatar = '/profile/avatar/2026/06/10/admin_avatar.png' WHERE user_name = 'admin';
```

### 3. 创建student123账号
```sql
INSERT INTO sys_user (user_id, user_name, nick_name, password, dept_id, email, phonenumber, sex, status, create_by, create_time)
VALUES (200, 'student123', '学生', '$2a$10$AjNH28sVK/NJH2KSTFxNLujOnVYKnMhij/QTk/N4l.NY3Pnjnupo2', 103, 'student@test.com', '13800000001', '0', '0', 'admin', NOW());

INSERT INTO sys_user_role (user_id, role_id) VALUES (200, 200);
```

---

## 三、测试账号信息

| 用户名 | 密码 | 角色 | 状态 |
|--------|------|------|------|
| admin | admin123 | 管理员 | 正常 |
| teacher123 | 123456 | 教师 | 正常 |
| student123 | 123456 | 学生 | 正常 |

---

## 四、功能测试结果

| 功能模块 | admin | teacher123 | student123 |
|----------|-------|------------|------------|
| 登录功能 | ✅ | ✅ | ✅ |
| 实验室列表 | ✅ 5个 | ✅ 5个 | ✅ 5个 |
| 预约列表 | ✅ 7条 | ✅ 7条 | ✅ 7条 |
| 教学占用 | ✅ 3条 | ✅ 3条 | ❌ 无权限 |
| 维修管理 | ✅ 5条 | ✅ 1条 | ✅ 0条 |
| 用户管理 | ✅ 10人 | ❌ 无权限 | ❌ 无权限 |

---

## 五、系统访问地址

- **前端**: http://localhost:80/
- **后端API**: http://localhost:8080/

---

## 六、未修改的项目（需要大范围重构）

以下项目因涉及包名、数据库名等核心架构，修改需要较大范围重构，暂未修改：

- Java包名 `com.meeting.*`（34个文件）
- 数据库名 `meeting_hub`
- Maven模块名 `meeting-system`
- MyBatis XML namespace

---

## 七、启动命令

### 启动后端
```bash
export JAVA_HOME="/c/Program Files/Eclipse Adoptium/jdk-8.0.482.8-hotspot"
cd "c:/Users/程书岑/Desktop/实验室预约管理系统"
java -jar ruoyi-admin/target/ruoyi-admin.jar
```

### 启动前端
```bash
cd "c:/Users/程书岑/Desktop/实验室预约管理系统/ruoyi-ui"
NODE_OPTIONS=--openssl-legacy-provider npm run dev
```

### 启动Redis
```bash
net start Redis
```

---

## 八、注意事项

1. 验证码已启用，登录时需要输入验证码
2. 数据库使用 `meeting_hub`，用户名 `root`，密码 `123456`
3. 前端使用 Node.js，需要 `NODE_OPTIONS=--openssl-legacy-provider` 参数
